/**
 * Bridge domain constants.
 * Used exclusively by main-process bridge modules.
 */

/** Delay before reconnecting to the bridge WebSocket */
export const RECONNECT_DELAY_MS = 2_000

/** Timeout for forwarded JSON-RPC requests */
export const RPC_TIMEOUT_MS = 60_000
